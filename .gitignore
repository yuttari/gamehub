# dependencies
node_modules/

# next.js build / static export output
.next/
out/

# env (keep .env.example tracked, ignore local secrets)
.env*.local

# project data / memory (do not publish)
.workbuddy/

# reference source for the import script (not needed to build/deploy the site)
_src/

# logs
*.log
npm-debug.log*

# local dev / debug artifacts (never publish)
.playwright-cli/
sm_*.png
sm.yaml
test_below.js
nul

# private working documents belonging to other projects — do not publish
_doc_extract*.txt
数据治理*.md
_dist/
_fixes_delivery/
code-report.html
